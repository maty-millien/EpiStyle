{
  "name": "epitech-vs-coding-style",
  "displayName": "EpiStyle",
  "description": "Real-time coding style checker for Epitech students. Helps students maintain consistent code quality and follow Epitech standards.",
  "icon": "assets/icon.png",
  "version": "0.3.1",
  "publisher": "maty-millien",
  "license": "MIT",
  "author": {
    "name": "Maty MILLIEN",
    "url": "https://github.com/maty-millien"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/maty-millien/EpiStyle"
  },
  "engines": {
    "vscode": "^1.106.0"
  },
  "categories": [
    "Linters",
    "Formatters",
    "Programming Languages",
    "Education"
  ],
  "keywords": [
    "epitech",
    "coding-style",
    "c",
    "cpp",
    "formatter"
  ],
  "activationEvents": [
    "onLanguage:c",
    "onLanguage:makefile",
    "onLanguage:cpp"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [],
    "configuration": {
      "title": "Epitech Coding Style Real-Time Checker",
      "properties": {
        "epitech-coding-style.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable the real-time coding style checker."
        }
      }
    }
  },
  "scripts": {
    "build": "bun run build.ts",
    "build:prod": "NODE_ENV=production bun run build.ts",
    "typecheck": "tsc --noEmit",
    "watch": "bun run --watch build.ts",
    "clean": "rm -f *.vsix && rm -rf out",
    "compile": "bun install && bun run build",
    "deploy": "bun run compile && vsce package",
    "publish": "bun run deploy && vsce publish && bun run clean",
    "test": "bun run clean && bun run compile && code-insiders --extensionDevelopmentPath=$PWD"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.10.1",
    "@types/vscode": "^1.106.1",
    "@typescript-eslint/eslint-plugin": "^8.48.1",
    "@typescript-eslint/parser": "^8.48.1",
    "@vscode/vsce": "^3.7.1",
    "bun-types": "^1.3.3",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3"
  }
}
