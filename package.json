{
  "name": "epitech-vs-coding-style",
  "displayName": "EpiStyle",
  "description": "Real-time coding style checker for Epitech students. Helps students maintain consistent code quality and follow Epitech standards.",
  "icon": "assets/icon.png",
  "version": "0.5.0",
  "publisher": "maty-millien",
  "license": "MIT",
  "author": {
    "name": "Maty MILLIEN",
    "url": "https://github.com/maty-millien"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/maty-millien/EpiStyle"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "scripts": {
    "build": "bun build ./src/extension.ts --outdir ./out --target node --format cjs --external vscode --sourcemap=external",
    "typecheck": "tsc --noEmit",
    "watch": "bun build ./src/extension.ts --outdir ./out --target node --format cjs --external vscode --sourcemap=external --watch",
    "clean": "rm -f *.vsix && rm -rf out",
    "compile": "bun install && bun run build",
    "test": "bun run clean && bun run compile && code --extensionDevelopmentPath=$PWD",
    "format": "eslint --fix \"src/**/*.ts\" \"scripts/**/*.ts\" && prettier --write \"src/**/*.ts\" \"scripts/**/*.ts\" \"*.{json,md,js,mjs,cjs,ts,yml,yaml}\"",
    "release": "bun run scripts/release.ts"
  },
  "categories": [
    "Linters",
    "Education"
  ],
  "keywords": [
    "epitech",
    "coding-style",
    "c",
    "linter",
    "checker",
    "style"
  ],
  "activationEvents": [
    "onLanguage:c",
    "onLanguage:makefile"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "epitech-coding-style.restartContainers",
        "title": "EpiStyle: Restart Checker Containers"
      }
    ],
    "configuration": {
      "title": "Epitech Coding Style Real-Time Checker",
      "properties": {
        "epitech-coding-style.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable the real-time coding style checker."
        },
        "epitech-coding-style.persistLogFile": {
          "type": "boolean",
          "default": false,
          "description": "Keep the .vscode/coding-style-reports.log file after analysis. If disabled, the file is deleted as soon as the analysis finishes."
        },
        "epitech-coding-style.excludePaths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "tests/"
          ],
          "description": "Gitignore-style patterns to exclude from analysis. Applied in addition to the workspace .gitignore."
        }
      }
    }
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.5.2",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^8.58.1",
    "@typescript-eslint/parser": "^8.58.1",
    "@vscode/vsce": "^3.7.1",
    "bun-types": "^1.3.11",
    "eslint": "^10.2.0",
    "prettier": "^3.6.2",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "ignore": "^7.0.5"
  }
}
